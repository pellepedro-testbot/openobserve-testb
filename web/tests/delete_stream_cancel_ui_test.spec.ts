// Generated by Skyramp v1.3.29 on 2026-06-25 15:30:25.928796746 +0000 UTC m=+636.721250660
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output delete_stream_cancel_ui_test.spec.ts \
// 		--output-dir /home/runner/work/openobserve-testb/openobserve-testb/web/tests \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/openobserve-testb/openobserve-testb/.skyramp/delete_stream_cancel_trace.zip

// Import of required libraries
import { test, expect, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(491000);

    const errors: Error[] = [];
    page.on('pageerror', e => errors.push(e));

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto("http://localhost:5080/web");
    // Wait for network activities to finish
    await page.waitForTimeout(5000);

    await page.locator("[data-test=\"login-user-id-field\"]").fill("root@example.com");
    await page.locator("[data-test=\"login-password-field\"]").fill("Complexpass#123");
    await page.locator("[data-test=\"login-sign-in\"]").click();
    await page.waitForLoadState('networkidle');
    await page.goto("http://localhost:5080/web/streams?org_identifier=default");
    // Wait for network activities to finish
    await page.waitForTimeout(5000);

    await page.locator("[data-test=\"log-stream-delete-btn\"]").click();
    await expect(page.locator("[data-test=\"o-dialog-secondary-btn\"]")).toBeVisible();
    await page.locator("[data-test=\"o-dialog-secondary-btn\"]").click();
    await expect(page.getByText("Stream(s)")).toHaveText("1 Stream(s)");
    expect(errors).toHaveLength(0);
});
